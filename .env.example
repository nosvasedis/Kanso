# GitHub personal access token with "gist" scope (Settings → Developer settings → Fine-grained or classic PAT).
# Copy this file to .env and paste your token. .env is gitignored — never commit it.
GITHUB_TOKEN=
